/**
 * Renders the application's home page wrapper containing the text "Home".
 *
 * @returns A React element: a `div` with className `"flex min-h-screen home-wrapper"` containing the text `"Home"`.
 */
export default function Home() {
	return <div className='flex min-h-screen home-wrapper'>Home</div>
}